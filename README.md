Possible options
    # -p  <name>   : switch to playlist name: 
    # -ar <name>   : play from artist portfolio
    # -a  <name>   : play from album portfolio
    # -q <name> <artist>   : add song <name> to queue
    # -h           : list all possible options
    # --login      : user enters login details
    # --userConfig : show/modify user config



debated using argparse lib but i didnt like